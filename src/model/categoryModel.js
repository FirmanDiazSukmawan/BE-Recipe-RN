// const db = require("../config/db");

// const readCategory = () => {
//     return db.query("SELECT * FROM category ");
// };

// const findCategoryId = (id) => {
//     return db.query(`SELECT * FROM category WHERE category.id =${id}`);
// };

// const createCategory = (name) => {
//     return db.query(`INSERT INTO category (name) VALUES ('${name}')`);
// };

// const updateCategory = (name, id) => {
//     return db.query(`UPDATE category SET name='${name}' WHERE category.id = ${id}`);

// };

// const deleteCategory = (id) => {
//     return db.query(`DELETE FROM category WHERE category.id = ${id}`);
// };






// module.exports = { readCategory, findCategoryId, createCategory, updateCategory, deleteCategory };